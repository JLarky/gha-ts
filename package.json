{
  "name": "gha-ts",
  "version": "0.1.0",
  "description": "TypeScript port of pkl-gha to generate GitHub Actions YAML",
  "type": "module",
  "license": "MIT",
  "private": true,
  "scripts": {
    "test": "bun test",
    "format": "oxfmt src tests",
    "format:check": "oxfmt --check src tests"
  },
  "engines": {
    "bun": ">=1.1.0"
  },
  "devDependencies": {
    "@types/node": "^24.8.1",
    "bun-types": "^1.1.0",
    "oxfmt": "^0.5.0",
    "yaml": "^2.8.1"
  }
}
